# agent-distro

Cerebro as an installable agent distro — a private distribution of agent behavior for one operator.

**Active development is on the [`v2`](../../tree/v2) branch.** `main` is intentionally bare and stays
that way until v2 has something worth releasing.
